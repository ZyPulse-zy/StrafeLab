# StrafeLab 1.7 · Tactical Analytics

## 方向与范围

目标：让玩家在本地桌面上先看懂长期交接变化，再决定是否做键盘参数对照。用户从三套方案中明确选择 A：黑底、黄绿强调、大数字、非对称战术分析台。

采用 ilm-alan-frontend-design 的 Industrial 方向与 beautiful-ui 的设计流程，按 ui-verification 复核。最有辨识度的结构是左侧巨幅交接波动读数与右侧贯通的统计画布。顶部功能页签取代侧栏；区块依靠线条、比例和字重区分。全界面无圆角卡片堆叠、渐变、装饰性光晕、拟造指标或持续动画。

## 设计系统

- 底色 #0B0C0A，工作面 #11130F / #181A17，分隔 #34392E。
- 文字 #E8EBDD，辅助文字 #A0A795，唯一信号色 #C6FF4A；选中面 #29361B。错误同时用“未保存 / 失败”等文字说明，颜色不是唯一状态线索。
- 字体为应用内嵌 IBM Plex Mono 与 Sarasa Mono SC 的 Regular / SemiBold；不需要安装到 Windows。标题、数字、中文说明均使用等宽系统。来源与 OFL 许可见 THIRD-PARTY-NOTICES.md。
- 字号 12 / 13 / 17 / 25 / 29 / 76 DIP，主读数较长时缩小；行高与窗口采用 WPF 逻辑像素。主图刻度使用 10–12 DIP。
- 4 / 8 / 12 / 16 / 24 间距；直角；1 DIP 分隔，2 DIP 当前页底线。按钮、输入框、列表都有焦点、悬停、选中和禁用状态。
- 无自动动效；方向键与 Enter 可操作主趋势图，逐局列表提供等价入口。

## 页面与信息层级

1. 长期趋势：时间与同类动作筛选 → 波动 / 中位数 / 样本读数及逐局主图 → 可读变化 → 带零线的完整交接直方图与方向比较 → 参数对照入口。辅助时长、口径和逐局明细按需展开或向下查看。
2. 比赛复盘：保留武器、姿态、初速及对局筛选、排除原因、CSV 导出和三轨按键时间线。新主题覆盖表格与图形。
3. 键盘参数：建议依据、实际方案记录、前后对照、旧局归属；切页保留草稿，字段错误保留输入。参数修改仍在官方驱动进行。
4. Demo 资料库：导入、扫描、任务状态与搜索、具体失败原因、目录管理。
5. 采集设置：后台状态、登录启动、Hall、资源实测、缓存预览与清理。

## 统计呈现契约

- 首页读数是当前时间范围和同组所有有效完整交接的统计；曲线显示其中最近 12 局。不会把逐局中位数再平均来冒充总体分布。
- 交接时间沿用既有定义：同按时长减空档时长。负值是空档，正值是同按；零值与缺失分别处理。
- 主图默认画逐局中位数和中间 50% 范围。实心点至少有 5 次该指标的有效样本；不足 5 次用空心点，连续曲线和色带不跨越这些点或缺失。
- 独立的“交接波动”视图显示每局 IQR，不为 IQR 再编造区间。少于两次完整交接不显示波动数值。
- 直方图包含完整的有限样本范围，不截除长尾。方向列表使用相同来源并明确毫秒单位。
- 方案分界只根据相邻两局均明确的不同方案 ID 绘制；名称重复也能区分，未知记录不会被猜测为调参。
- 沿用可信配对、原方向运动、初速 ≥34 u/s 等资格检查；蹲伏与慢走分别保留。没有改变物理模型、Hall 命令、采集输入或参数建议的因果边界。

## 状态、桌面行为与验证

- 无数据：大读数为“—”，数量为真实的 0；显示采集 / 导入入口，主图不填假波形。
- 少样本：显示基线说明与空心点；不把一次动作的零跨度当成稳定。
- 无搜索结果、解析失败、读取失败、参数校验失败：保留原因及可用的恢复操作。
- Windows 原生窗口、缩放、拖动与系统按钮；支持 DWM 暗色标题栏。最小 960×700 DIP，宽表保留水平滚动。
- 本地程序无前端框架、浏览器渲染依赖或联网字体请求。
- 发布验收使用真实报告的隔离副本、空数据和单样本，验证页面、图表键盘选择、筛选、未保存草稿、方案保存和宽度重排。截图来自实际 WPF 控件渲染，不是生成式效果图。
- 检查不在真实数据目录试删，不自动修改键盘设置。具体构建、交互、发布验证记录见 VERIFICATION.md。
