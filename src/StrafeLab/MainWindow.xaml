<Window x:Class="StrafeLab.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="clr-namespace:StrafeLab.UI"
        mc:Ignorable="d"
        Title="StrafeLab"
        Width="1280"
        Height="820"
        MinWidth="1050"
        MinHeight="700"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{StaticResource WindowBrush}"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    <Border Background="{StaticResource WindowBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="64" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#0E192A" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="24,0,12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="30" Height="30" CornerRadius="9" Background="#1B5E87" Margin="0,0,10,0">
                            <TextBlock Text="S" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" />
                        </Border>
                        <StackPanel>
                            <TextBlock Text="StrafeLab" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock Text="CS2 movement observatory" FontSize="11" Foreground="{StaticResource MutedBrush}" />
                        </StackPanel>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,24,0">
                        <Ellipse x:Name="statusDot" Width="8" Height="8" Fill="{StaticResource GreenBrush}" Margin="0,0,8,0" />
                        <TextBlock x:Name="topStatusText" Text="就绪" FontSize="12" Foreground="{StaticResource MutedBrush}" />
                    </StackPanel>
                    <Button Grid.Column="2" Content="—" Width="40" Height="32" Padding="0" Margin="0,0,6,0" Click="Minimize_Click" />
                    <Button Grid.Column="3" Content="×" Width="40" Height="32" Padding="0" Click="Close_Click" />
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="224" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#0D1727" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0" Padding="18,20">
                    <DockPanel>
                        <StackPanel DockPanel.Dock="Bottom">
                            <TextBlock Text="数据完全保存在本机" FontSize="11" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,14,0,4" />
                            <TextBlock x:Name="dataPathText" Text="%LOCALAPPDATA%\\StrafeLab" FontSize="10" Foreground="#58708F" TextWrapping="Wrap" />
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="WORKSPACE" FontSize="10" FontWeight="SemiBold" Foreground="#58708F" Margin="4,0,0,12" />
                            <Button Content="◉  实时分析" HorizontalContentAlignment="Left" Background="#19314E" BorderBrush="#2D6B9A" />
                            <Button Content="▣  历史局" HorizontalContentAlignment="Left" Click="History_Click" />
                            <Button Content="⌁  训练反馈" HorizontalContentAlignment="Left" Click="Feedback_Click" />
                            <Button Content="⚙  连接与诊断" HorizontalContentAlignment="Left" Click="Diagnostics_Click" />
                            <Separator Margin="0,12" Background="{StaticResource BorderBrush}" />
                            <TextBlock Text="QUICK ACTIONS" FontSize="10" FontWeight="SemiBold" Foreground="#58708F" Margin="4,0,0,12" />
                            <Button x:Name="recordButton" IsEnabled="False" Content="开始记录" Style="{StaticResource PrimaryButton}" Click="Record_Click" />
                            <Button Content="安装 CS2 GSI" Click="InstallGsi_Click" />
                            <Button Content="打开数据目录" Click="OpenData_Click" />
                            <Button Content="手动选择 CS2 cfg" Click="SelectCfg_Click" />
                            <CheckBox x:Name="autoRecordBox" Content="GSI 自动记录每局" IsChecked="True" Foreground="#CAD7ED" Margin="0,12,0,8" Checked="Modes_Changed" Unchecked="Modes_Changed"/>
                            <CheckBox x:Name="diagnosticBox" Content="桌面诊断模式" Foreground="#CAD7ED" Checked="Modes_Changed" Unchecked="Modes_Changed"/>
                            <CheckBox x:Name="hallEnabledBox" IsEnabled="False" Content="Hall 键程监测" IsChecked="True" Foreground="#CAD7ED" Margin="0,8,0,0" Checked="HallMode_Changed" Unchecked="HallMode_Changed"/>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="26,24,28,32">
                        <Grid Margin="0,0,0,22">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="实时急停分析" FontSize="28" FontWeight="SemiBold" />
                                <TextBlock Text="记录输入边沿，估算速度，并把每次 Mouse1 和反向切换对齐到比赛状态。" FontSize="13" Foreground="{StaticResource MutedBrush}" Margin="0,7,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                <TextBlock x:Name="matchText" Text="等待 CS2" FontSize="13" HorizontalAlignment="Right" />
                                <TextBlock x:Name="weaponText" Text="武器 — · 回合 —" FontSize="11" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Right" Margin="0,5,0,0" />
                            </StackPanel>
                        </Grid>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="Hall 连续键程" FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock x:Name="hallStatusText" Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,5,0,12"/>
                                <UniformGrid Columns="4">
                                    <StackPanel Margin="0,0,12,0"><TextBlock x:Name="hallWText" Text="W  —"/><ProgressBar x:Name="hallW" Maximum="3.41" Height="8" Margin="0,8,0,0" Foreground="#47C7FF" Background="#17263B"/></StackPanel>
                                    <StackPanel Margin="0,0,12,0"><TextBlock x:Name="hallAText" Text="A  —"/><ProgressBar x:Name="hallA" Maximum="3.41" Height="8" Margin="0,8,0,0" Foreground="#47C7FF" Background="#17263B"/></StackPanel>
                                    <StackPanel Margin="0,0,12,0"><TextBlock x:Name="hallSText" Text="S  —"/><ProgressBar x:Name="hallS" Maximum="3.41" Height="8" Margin="0,8,0,0" Foreground="#47C7FF" Background="#17263B"/></StackPanel>
                                    <StackPanel><TextBlock x:Name="hallDText" Text="D  —"/><ProgressBar x:Name="hallD" Maximum="3.41" Height="8" Margin="0,8,0,0" Foreground="#47C7FF" Background="#17263B"/></StackPanel>
                                </UniformGrid>
                            </StackPanel>
                        </Border>
                        <UniformGrid Columns="4" Margin="0,0,0,20">
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="输入可信样本" FontSize="11" Foreground="{StaticResource MutedBrush}" />
                                    <TextBlock x:Name="qualityText" Text="—" FontSize="28" FontWeight="SemiBold" Margin="0,8,0,0" />
                                    <TextBlock Text="按键边沿完整率" FontSize="11" Foreground="#58708F" Margin="0,4,0,0" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="反向切换平均 gap" FontSize="11" Foreground="{StaticResource MutedBrush}" />
                                    <TextBlock x:Name="gapText" Text="0.0 ms" FontSize="28" FontWeight="SemiBold" Margin="0,8,0,0" />
                                    <TextBlock x:Name="overlapText" Text="overlap 0.0 ms" FontSize="11" Foreground="#58708F" Margin="0,4,0,0" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="Mouse1 时估算速度" FontSize="11" Foreground="{StaticResource MutedBrush}" />
                                    <TextBlock x:Name="shotSpeedText" Text="0 u/s" FontSize="28" FontWeight="SemiBold" Margin="0,8,0,0" />
                                    <TextBlock x:Name="shotDeltaText" Text="timing 0.0 ms" FontSize="11" Foreground="#58708F" Margin="0,4,0,0" />
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="反向切换次数" FontSize="11" Foreground="{StaticResource MutedBrush}" />
                                    <TextBlock x:Name="sampleText" Text="0" FontSize="28" FontWeight="SemiBold" Margin="0,8,0,0" />
                                    <TextBlock x:Name="windowText" Text="低移速估计 · 等待样本" TextWrapping="Wrap" FontSize="11" Foreground="#58708F" Margin="0,4,0,0" />
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock Text="估算速度" FontSize="14" FontWeight="SemiBold" />
                                        <TextBlock x:Name="speedNowText" Text="0 u/s" FontSize="13" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Right" />
                                    </Grid>
                                    <ui:Sparkline x:Name="speedChart" Height="178" Margin="0,16,0,0" />
                                    <Grid Margin="0,12,0,0">
                                        <TextBlock Text="0" FontSize="10" Foreground="#58708F" />
                                        <TextBlock Text="estimated ground model · 128 Hz substeps" FontSize="10" Foreground="#58708F" HorizontalAlignment="Center" />
                                        <TextBlock Text="250 u/s" FontSize="10" Foreground="#58708F" HorizontalAlignment="Right" />
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="链路状态" FontSize="14" FontWeight="SemiBold" />
                                    <TextBlock x:Name="deviceChainText" Text="HID 探测中…" FontSize="12" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,14,0,0" />
                                    <Separator Margin="0,14" Background="{StaticResource BorderBrush}" />
                                    <TextBlock x:Name="gsiChainText" Text="GSI 未连接" FontSize="12" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,0,0,9" />
                                    <TextBlock x:Name="demoChainText" Text="Demo 自动匹配待开始" FontSize="12" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" />
                                    <Border Background="#17263B" CornerRadius="8" Padding="10" Margin="0,18,0,0">
                                        <TextBlock Text="保护模式：只观察输入和 GSI，不写入游戏，不读内存，不注入输入。" FontSize="11" Foreground="{StaticResource GreenBrush}" TextWrapping="Wrap" />
                                    </Border>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                            <StackPanel>
                                <Grid Margin="0,0,0,12">
                                    <TextBlock Text="最近反向切换" FontSize="14" FontWeight="SemiBold" />
                                    <TextBlock x:Name="transitionCountText" Text="0 events" FontSize="11" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Right" />
                                </Grid>
                                <ListBox x:Name="transitionList" Height="162" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextBrush}" />
                            </StackPanel>
                        </Border>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,10,0">
                                <StackPanel>
                                    <TextBlock Text="本地数据" FontSize="14" FontWeight="SemiBold" />
                                    <TextBlock Text="每局结束后写入 JSON，包含输入边沿、GSI、模型样本和校准结果。" FontSize="12" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,10,0,0" />
                                    <TextBlock x:Name="saveStatusText" Text="尚未开始记录" FontSize="11" Foreground="#58708F" Margin="0,12,0,0" />
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="模型边界" FontSize="14" FontWeight="SemiBold" />
                                    <TextBlock Text="GSI 不提供完整接地/速度状态；Demo 同步成功且样本置信度足够时才会修改模型。" FontSize="12" Foreground="{StaticResource MutedBrush}" TextWrapping="Wrap" Margin="0,10,0,0" />
                                    <TextBlock Text="低置信度样本会保留在记录里，但不会进入校准。" FontSize="11" Foreground="{StaticResource WarnBrush}" Margin="0,12,0,0" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Border>
</Window>
