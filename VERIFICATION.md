# 1.7 战术分析台验证（2026-09-27）

- Release 编译成功；110/110 测试通过。新增聚合读数、方向与缺失边沿、已确认方案 ID 的检验。
- 真实报告隔离副本：默认 AK-47 / 站姿 / 初速 ≥150 组为 9 局、70 次完整交接；主读数 38.3 ms，中位数 +2.3 ms。这是验证时的本地历史快照，不是固定内置数据。
- 主趋势图五种指标均渲染；键盘方向键 / Enter 跳转、表单草稿、错误输入保留、任务筛选与无结果恢复通过。空数据不生成数值；单样本显示空心点，波动为“—”。
- 验证常规窗口、1040 宽度、960×720 小窗口；96 / 144 DPI 为控件渲染检查，不代替混合 DPI 显示器实机验收。
- 默认正文对比 16.20:1，辅助文字 7.05:1，强调与按钮 16.62:1。读屏名称沿用，尚未进行完整辅助技术或用户研究验收。
- 隔离验收结束时，真实 sessions / analysis / 参数记录共 35 个文件的 SHA-256 与改造前一致。随后启动正式程序，原有后台扫描刷新了 8 份未配对报告的 updatedAtUtc；逐字段核对确认仅时间戳变化，动作、统计、原始记录与参数内容保留。所有 UI 保存流程在隔离目录执行。
- 自包含发布目录 `outputs/StrafeLab-1.7` 启动检查退出码为 0，五种图表指标、键盘与筛选检查通过；内置 Python 成功导入 DemoParser。发布窗口证据在 `work/ui-17-release`。
- 工程证据：`work/ui-17-final`、`work/ui-17-empty`、`work/ui-17-one` 和 `work/ui-17-verification`。包校验见 outputs/package-verification-1.7.json；本机安装与启动回读见 work/ui-17-verification。
- 设计确认图只是视觉参考。最终界面截图来自实际 WPF 渲染；没有新增游戏输入、内存读取、注入、键盘参数或灯光写入。

# 1.6 界面重构验证（2026-09-26）

- Release 编译无警告/错误，108 / 108 现有测试通过。统计资格、Demo 同步、后台采集和缓存边界没有修改。
- WPF 正式控件使用真实报告的隔离副本、单局副本、空记录三种数据状态运行；没有修改真实参数、会话或报告。
- 界面脚本验证同组到动作的跳转、返回筛选、页面切换保留草稿、无效行程不写入且保留输入、图表方向键/Enter 与可访问名称、Demo 状态筛选、搜索空结果与恢复、武器/姿态/排除筛选、方案保存与归属修改。
- 1320×940、1040 宽度、960×720 离屏截图逐页检查。修正了控件主题文字颜色、筛选重排和时间线标记；宽表允许横向滚动。
- 自包含 1.6 发布版完成上述界面流程，退出码 0；隔离夹具完成 64 MiB 清理并保留索引。17 份真实会话、17 份真实报告及参数/采集设置/GSI token 共 37 个文件前后 SHA-256 相同。
- 以上属于工程与截图验收，不是用户可用性实验、完整辅助技术认证、下一次登录启动或实战效果验证。

以下为此前版本验证历史。

---

# 1.5.1 缓存清理验证（2026-09-26）

- Release 测试 108 / 108 通过。新增缓存删除范围、预览后文件变化、原压缩包缺失、文件使用中、用户主动导入、路径伪造、目录及文件链接、任务锁竞争、游戏运行时拒绝清理等边界测试。
- 使用正式解压代码建立夹具；清理后原始压缩包、会话、报告、同步索引和队列逐字节保持一致；原压缩包可再次解压得到同一文件。
- 隔离 WPF 界面完成 64 MiB 缓存清理，剩余 2 B 索引；实际释放量、禁用清理按钮、占用刷新均通过。1320 / 1040 宽度截图检查未发现遮挡。测试未对真实缓存执行删除。
- 本机真实缓存的只读预览成功：来源检查能对应已有压缩包，提供可释放量；实际删除需用户在界面确认。

以下为此前版本的验证历史。

---

# 1.5 验证补充（2026-09-26）

- `dotnet test StrafeLab.sln -c Release`：95 / 95，通过；新增空闲不创建采集源、游戏进程生命周期、暂停恢复、初始化失败重试、用户级 IPC、Demo 游戏中等待、流式存储、状态过期、互不重叠多局趋势与交接顺序缺失值测试。
- .NET 8 Windows x64 自包含发布成功。WPF 使用真实历史报告的隔离副本，验证五个侧栏页面、武器/姿态筛选、趋势到动作跳转、键盘方案保存和历史归属。1320 / 1040 宽度均生成截图并检查；深色下拉框可读，动作跳转能显示按键时间线。测试参数只写隔离目录。
- 托盘实机空闲测试：重复启动退出；pause / resume 生效；Hall 偏好 off / on 持久化与状态回读通过；正常退出响应 ok，状态文件移除。没有启动 CS2 或产生游戏输入。
- 10 秒空闲操作后采样：工作集 84.30–95.79 MiB，专用内存 28.09–39.80 MiB，累计 CPU 0.2656 秒。这是单台机器短时结果，包含一次元数据检查，不能代表长局峰值或所有硬件。
- 从空队列和隔离下载目录发现一份实际完美 ZIP，后台 worker 完成解压、解析、与本地记录对齐：41 候选、38 匹配、27 合格，可靠同步残差 29.9531 ms。worker 自动退出，无错误日志。未复用该隔离目录的既有报告或解析缓存。
- 安装到 `%LOCALAPPDATA%/Programs/StrafeLab`，创建桌面 `StrafeLab.lnk`，登记当前用户 HKCU Run `StrafeLab`，参数 `--collector`。登记前的启动值、快捷方式或旧程序存在时保存在数据目录的 install-backup 中。
- 原有 12 份会话文件 SHA-256 前后相同。实际键盘参数文件未由测试写入。
- 尚未进行下一次 Windows 登录、游戏实战 FPS/输入延迟对照、长局内存峰值或注销中断保存的真实验收。Hall 原有固件验证边界不变；自启动登记与手动启动成功不代替这些验收。

以下为此前版本的验证历史。

---

# 1.4 更新验证（2026-09-25）

- Release 构建无警告 / 错误，84 项 C# 测试通过。新增验证：同武器 / 姿态 / 初速分组、34 门槛、未知边沿不补零、分位数计算、趋势最少样本、方案数据不足时不判断优劣、方案不倒推旧局、中途变更不自动归属、未知方案不进入方案对比、保存 / 备份 / 损坏文件不覆盖。
- 首页改为进步与键盘参数，展示交接波动、两键同按、双键空档、换向到开枪的逐局变化；旧技术表格移到动作页。初速统计口径仍为报告 v4，没有改变样本资格。
- 使用两份已验证 Demo 报告渲染：默认跨局最多的一组为 Glock-18 / 站姿 / 初速 ≥150，共 13 次、2 局；首末局交接常见波动从 13 ms 到 60 ms，按描述性变化展示，没有报告命中率或将变化归因 RT。
- 发布版 WPF 启动、渲染、正常退出通过（exit 0）。真实截图核对首页、参数编辑器、动作时间线；控件测试验证趋势到所选对局 / 武器 / 姿态 / 初速的跳转、详情范围不改变汇总、已排除边界样本仍显示原因。
- 独立测试目录完成方案保存、指定旧局方案、再标为未知的完整界面流程。测试仅修改隔离目录，不把测试方案或截图预填值写入真实参数历史。
- 跳转动作页时先等待 Tab 内容完成布局，再按可滚动高度跳到时间线，避免偶发停在页面顶部。窗口初始大小限制在当前工作区内。
- 参数建议仍是记录基线、是否开始单项对照，以及同组方案的描述性比较；没有最佳 RT 的实战验证，也没有物理因果或统计显著性结论。截图预填是待用户确认的历史信息，不是推荐值。程序未增加键盘配置写入能力。

以下为历史验证记录，初速门槛仍按 1.3.1 执行。

---

# 1.3.1 更新验证（2026-09-25）

- 76 项 C# 测试通过，Release 发布成功。边界覆盖 33.999 排除、34 保留、34.001 保留，以及站姿、全蹲、蹲起和慢走的同等判断；验证使用前三 tick 的水平总速度中位数，而非最小值、最大值、单轴速度或开枪速度。开枪仍快的合格动作照样保留。
- 统计初速门槛恢复为 ≥34 u/s；姿态分类不作为直接排除条件。低于门槛的候选保留明细，但不进入共享的资格集合、分组、时序中位数与趋势。
- 两份真实 Demo 经正式后台队列重新解析：官匹 54 候选，43→41 可评估（36 站姿、3 慢走、2 蹲起）；完美 41 候选，30→27 可评估（20 站姿、5 慢走、2 蹲起）。逐条比对新旧报告，候选、姿态、原始时序、速度均一致，唯一新增排除条件为初速 <34。
- 完美的一次 AWP 初速 33.994751469 u/s 被正确排除；详情显示 33.9948 并说明使用未四舍五入值判断。导出包含初速中位数与门槛字段。
- 报告口径升级为 4。版本 1/2/3 旧报告失效、队列重新排队的测试通过；原始会话及 Demo 不修改，自动模型校准仍关闭。
- 发布后的 WPF 分析窗口在独立目录完成启动、渲染及退出（exit 0）；筛选和汇总断言通过，已检查边界样本排除原因与初速详情截图。期间修正了切换明细范围、仍保留同一选择时详情被提示文字覆盖的问题。测试未启动 HID / Raw Input / GSI。
- 本机分析中心已升级到 1.3.1，后台队列口径为 4，两局已验证报告已载入，其余任务继续后台处理。旧报告/队列已备份；6 份原始会话哈希一致，GSI 配置及 token 哈希和修改时间不变，原采集进程仍运行，实际错误日志无新增。

以下为历史验证记录：1.3 将初速 <34 纳入的规则及对应样本数已被 1.3.1 取代；姿态分组保留。不要把历史版本的统计混入当前结果。

---

# 1.3 更新验证（2026-09-24）

- 71 项 C# 测试通过。新增低初速 1 / 5 / 20 / 34 u/s 的蹲姿和慢走仍计入；跨旧 34 边界不排除；短按、轴向复合移动、接地坡面支持；缺运动证据仍拒绝，未知姿态单列。
- 按具体武器 × 姿态 × 起速分组，测试保证不同组不会改变同组数量，开枪仍快的动作照样保留。没有统一低速成功率。
- 两份真实 Demo 从原始会话重算：官匹 54 个候选中可评估 43 个（37 站姿、3 慢走、3 蹲起）；完美 41 个候选中可评估 30 个（21 站姿、5 慢走、4 蹲起）。这些是可评估反向动作数，不是成功数。
- 报告版本提升为 3；旧报告自动失效并重算，原始会话和 Demo 不修改，物理模型自动校准继续关闭。
- WPF 分析中心在独立测试目录成功渲染，武器/姿态筛选与只切换明细不改汇总的断言通过；截图核对了蹲起动作“计入”及近静止“未评估”的区别。测试窗口不获取焦点，也不启动 HID / Raw Input / GSI。
- 测试发现后台尚未启动时关闭窗口可能同步重入 Closing，已改为通过 Dispatcher 延后关闭；复测正常退出（exit 0）。分析中心 1.3 已启用，旧录制进程继续运行，旧报告/队列已备份。

以下为历史记录。1.2 的排除规则过严，已由 1.3 取代，不能与当前结果直接比较。

---

# 1.2 更新验证（2026-09-24）

- 66 项 C# 测试通过，包括蹲起前/中/后任一采样、旧输入蹲键、非步枪、原本低速、边界、不完整姿态/缺 tick、慢走/开镜/受击、再次换向、原方向不一致及蹲键释放后置信度。
- 测试明确验证成功和失败急停同时进入分母；增加低速蹲射/冲锋枪样本不改变主统计比例；没有合格样本返回未知。
- 统计口径版本提升至 2，旧报告需要重算；不修改原始会话和 Demo。
- 两份实际录像按新口径重算：官匹 54 个候选中 14 个合格（4 低速 / 6 仍在移动 / 4 边界）；完美 41 个候选中 3 个合格（2 低速 / 1 仍在移动）。排除原因保存在每条报告中，未按结果挑选样本。
- 实际 Demo 暴露的显示名称 AK-47 / M4A4 等已与 GSI 内部名称统一；增加 12 种名称映射用例，避免误判为武器不一致。
- 最终 WPF 发布版本真实启动、渲染与退出成功；查看全部候选/蹲伏明细不改变主统计数字，AK 筛选及蹲伏筛选断言通过。统计首页与未计入明细均检查截图。
- 本机已替换为 1.2 分析中心，并预先备份旧报告/队列；原 1.0 录制进程继续运行，原始会话逐文件哈希核对一致后才导入新报告。

以下保留历史验证记录，相关限制以 1.2 README 和 KNOWN-ISSUES 为准。

---

# 1.1 更新验证（2026-09-24）

- .NET Release 构建 0 警告 / 0 错误；32 项 C# 测试通过。
- 3 项 Python 时钟/速度测试通过：当前 tick 区间、跨玩家隔离、缺 tick 和缺时钟处理。
- 真实官匹 DEM 与此前本地记录自动配对；同地图另一场被拒绝。真实完美 ZIP 解压并匹配本机记录。
- 两份真实 Demo 均从 game_time 差分确认 64 Hz；旧速度字段的一 tick 偏移不再进入报告，指定事件区间速度与独立离线复盘吻合。
- BZ2 合法 Source2 数据往返一致，非 Demo 数据拒绝、临时输出清理；ZIP 路径穿越测试只写入指定缓存。
- 队列单个任务处理后再次扫描不重复解析，持久化记录保留结果；单元测试覆盖重启中断任务恢复、临时扩展名、稳定等待与文件变更。
- 新 WPF 页经过真实本地报告填充的截图检查；包含统计页、武器筛选和任务页。测试模式不启动 Hall、不写游戏 GSI 配置。
- 每局统计是有限筛选样本，不是整局所有子弹。自动物理校准暂不使用区间平均速度。
- 未证明所有下载器/平台、所有键盘固件、无限历史量、实战 FPS 或输入延迟表现；边界见 KNOWN-ISSUES.md。

以下保留 1.0 历史验证记录，相关限制以 1.1 README 和 KNOWN-ISSUES 为准。

---

# 验证记录

日期：2026-09-23。区分实机观测、自动化检查和未完成的实战验证。

## 已验证

- 实际 ACE68 Air-II 41E4:2120 的全部 3 个 HID interface / 7 个 collection 枚举，HIDP caps、报告编号、长度及 Windows 重建描述符已记录。
- 用户受控慢按 WASD 后，官方 WebHID 监测位启用时取得连续 A0 包。四键键程均为 0–341；不同值 A93 / D81 / W71 / S72。
- 官方网页离开设备页面后，独立 C# / HidSharp 实机读取获得 151 条 WASD 样本：A66、D30、W12、S43。正常退出恢复监测位，并清除恢复 journal。
- WebHID 实验的 64 字节功能配置恢复后与备份逐字节相同；C# 使用读回确认监测位恢复。
- Release 测试 **18/18 通过**：gap、两种先后释放顺序的 overlap、反向持键、Mouse1 timing、RT 与 Hall 分离、RawMouse 原生布局、真实 HID 数据包回放、命令白名单、GSI 缺字段与认证、对齐时钟漂移及外人射击排除、原子保存版本保护、解析解轨迹上的参数拟合与时间留出验证、低置信度/空中样本拒绝。
- demoparser 上游真实 CS2 `test_demo.dem`（60,601,900 字节）通过 Python→JSONL→C#：de_mirage / 64 tick，58,171 条记录，只含指定的一名玩家；其中接地且存活 49,321 条，正常移动类型 52,550 条，weapon_fire 170 条。
- WPF 真实启动与控件渲染检查成功，1280×820，GSI 自动避开不可绑定端口并使用 3002，正常退出。预览来自应用自身的控件渲染，不是效果图。
- 发布为 Windows x64 自包含 .NET 程序，包含独立 Demo 解析运行时。对最终发布目录再次启动检查：`initialized=true`，GSI 3002，1280×820，正常退出且无错误日志。内置 Python 对同一个真实 Demo 每 64 tick 抽样，成功解析 199 个事件和 906 个玩家样本（包含头尾共 1,107 行 JSONL），无需系统 Python。

## 尚未证明

- 尚没有一局“本用户本地 Raw Input / GSI 记录 + 对应 Demo”的真实联合数据。因此没有声称已对本用户 CS2 环境完成有效校准，也没有编造统计成绩。
- GSI 的本地 HTTP、认证、字段解析和配置安装已实现/测试；真实游戏内比赛状态、自动分局、长时间游戏负载下的输入延迟需要实际使用验证。
- 未做 debugMode 开/关情况下的 USB 输入延迟对照，Hall 监测速率不是标称键盘轮询率。
- 固件毫米值的绝对机械精度、其它固件/PID、灯光写入尚未验证。
- 实时移动模型不是完整 CS2 引擎，射击精度还受后坐力、碰撞、视角、姿态等影响；UI 的低移速窗口是保守估计。

测试命令：`dotnet test StrafeLab.sln -c Release`。本机硬件研究原始记录在工作目录 `work/hall`；交付证据中的 CSV 和 JSON 只保留 WASD 协议验证信息，不包含用户的 GSI token 或浏览器连接日志。
