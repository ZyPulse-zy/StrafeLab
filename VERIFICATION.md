# 验证记录

日期：2026-09-23。区分实机观测、自动化检查和未完成的实战验证。

## 已验证

- 实际 ACE68 Air-II 41E4:2120 的全部 3 个 HID interface / 7 个 collection 枚举，HIDP caps、报告编号、长度及 Windows 重建描述符已记录。
- 用户受控慢按 WASD 后，官方 WebHID 监测位启用时取得连续 A0 包。四键键程均为 0–341；不同值 A93 / D81 / W71 / S72。
- 官方网页离开设备页面后，独立 C# / HidSharp 实机读取获得 151 条 WASD 样本：A66、D30、W12、S43。正常退出恢复监测位，并清除恢复 journal。
- WebHID 实验的 64 字节功能配置恢复后与备份逐字节相同；C# 使用读回确认监测位恢复。
- Release 测试 **18/18 通过**：gap、两种先后释放顺序的 overlap、反向持键、Mouse1 timing、RT 与 Hall 分离、RawMouse 原生布局、真实 HID 数据包回放、命令白名单、GSI 缺字段与认证、对齐时钟漂移及外人射击排除、原子保存版本保护、解析解轨迹上的参数拟合与时间留出验证、低置信度/空中样本拒绝。
- demoparser 上游真实 CS2 `test_demo.dem`（60,601,900 字节）通过 Python→JSONL→C#：de_mirage / 64 tick，58,171 条记录，只含指定的一名玩家；其中接地且存活 49,321 条，正常移动类型 52,550 条，weapon_fire 170 条。
- WPF 真实启动与控件渲染检查成功，1280×820，GSI 自动避开不可绑定端口并使用 3002，正常退出。预览来自应用自身的控件渲染，不是效果图。
- 发布为 Windows x64 自包含 .NET 程序，包含独立 Demo 解析运行时。对最终发布目录再次启动检查：`initialized=true`，GSI 3002，1280×820，正常退出且无错误日志。内置 Python 对同一个真实 Demo 每 64 tick 抽样，成功解析 199 个事件和 906 个玩家样本（包含头尾共 1,107 行 JSONL），无需系统 Python。

## 尚未证明

- 尚没有一局“本用户本地 Raw Input / GSI 记录 + 对应 Demo”的真实联合数据。因此没有声称已对本用户 CS2 环境完成有效校准，也没有编造统计成绩。
- GSI 的本地 HTTP、认证、字段解析和配置安装已实现/测试；真实游戏内比赛状态、自动分局、长时间游戏负载下的输入延迟需要实际使用验证。
- 未做 debugMode 开/关情况下的 USB 输入延迟对照，Hall 监测速率不是标称键盘轮询率。
- 固件毫米值的绝对机械精度、其它固件/PID、灯光写入尚未验证。
- 实时移动模型不是完整 CS2 引擎，射击精度还受后坐力、碰撞、视角、姿态等影响；UI 的低移速窗口是保守估计。

测试命令：`dotnet test StrafeLab.sln -c Release`。本机硬件研究原始记录在工作目录 `work/hall`；交付证据中的 CSV 和 JSON 只保留 WASD 协议验证信息，不包含用户的 GSI token 或浏览器连接日志。
