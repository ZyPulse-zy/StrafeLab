# 已知问题与验证边界

截至 2026-09-24，本仓库保存当前可运行版本。这些问题已在后续审查中发现，尚未在应用中修复；现有测试通过不代表以下场景已覆盖。

## 记录与统计

- 暂停录制或重置分析器后，历史未完成的反向键持续时间可能被后续录制片段的释放事件补全；Mouse1 也可能关联到上一片段的反向切换。跨片段的持续时间与 timing 不应作为训练结论。
- 历史统计在没有符合模型置信度要求的开枪样本时，可能显示 0%，应改成“无有效样本”。
- 当前汇总没有完整区分正式回合、热身、冻结期、刀具和投掷物。按键切换次数并不等于有效急停次数。
- 输入置信度是启发式质量分档，不是经过测量的准确概率；“按键边沿完整率”标签也不能证明没有丢失事件。

## 移动模型与 Demo

- 模型无法从普通 GSI 获得连续视角、碰撞和完整接地状态。移动鼠标后置信度会降低，实战中可能没有足够的可信样本。不能通过单纯提高置信度数值解决这一限制。
- 34 u/s 仅是保守低速阈值，并非完整武器精度模型。实时估算不能代替 Demo 中的真实速度。
- Demo 解析已用真实公开 Demo 验证；“本地实战输入与对应 Demo 对齐并校准”的完整链路仍未完成真实配对验收。
- 多种锚点共用同一时钟拟合，尚未分别建模 GSI 传输延迟与 Mouse1/weapon_fire 延迟。合成测试表明，不同事件来源的固定延迟可能使同步失败。
- 自动搜索只覆盖部分常见目录，遗漏完美平台的 `%APPDATA%/Wmpvp/demo`；尚无可配置多目录、压缩包导入或平台自动下载。
- 候选查找受文件修改时间、候选数量和最近会话数量限制，延迟下载或移动过的 Demo 可能不会自动匹配。可在历史界面手动选择已解压的 `.dem` 文件；文件路径和修改时间本身不能证明比赛对应关系。
- Demo 应作为可选的事后验证来源。本地按键和 Hall 记录不要求每局下载 Demo，但缺少可靠配对数据时不能宣称完成速度校准。

## 设备与长期使用

- Hall 仅验证了 `41E4:2120`、固件 `0x0117`。设备失联后自动重连和旧键程显示的清理仍需完善，当前可关闭再开启 Hall 检测。
- debugMode 对 USB 延迟的影响、固件行程值的绝对机械精度、其他固件及灯光写入均未验证。
- 会话每 5 秒完整写入 JSON，历史界面读取完整记录；长时间运行与大量历史数据的性能仍需专项验证。

仓库不包含私人比赛记录、Demo、GSI token 或原始浏览器日志。`evidence/` 仅包含去标识的受控 WASD 协议验证资料。
