# 已知问题与验证边界（1.5）

2026-09-26 更新。此文描述当前实现，不把测试通过等同于所有实战场景已覆盖。

## 已修正

- 首页优先展示长期变化和参数方案。术语改成中文描述，提供逐局曲线、典型值 / 常见范围、可点击动作时间线；原始数值保留在明细中。
- 增加用户确认的键盘方案历史、未来对局自动关联、旧对局手动补记与未知标记。旧截图仅预填，不当成实际使用历史，不向 HID 写入参数。

- 1.3.1 恢复初速 ≥34 u/s 的统计门槛，取反向前三 tick 的水平总速度中位数。慢走、蹲伏、蹲起不直接排除；仍按具体武器 × 姿态 × 起速分组展示。低于门槛的候选保留原始时序和排除原因，不参与汇总、分组、趋势。等于 34 保留，按未四舍五入值判断。
- 反向轴速度与总速度分别展示，复合移动不因为另一轴还在移动就直接算失败。坡面/楼梯使用接地标志，不按 Z 速度误判成跳跃。
- 蹲键释放后不会立即恢复高模型置信度；一秒保守窗口内仍属估算不可靠，慢走亦单独降级。

- 暂停/重置后不再让新的释放或 Mouse1 填入上一片段；历史报告还会重查原始边沿及物理样本连续性。
- 无合格模型样本不再报告 0%；新统计页仅统计本人存活、live 回合、枪械、可信输入的反向后首击，缺失持键不填零。
- 新增完美、Steam、Downloads 目录及自定义目录；支持 DEM、ZIP、DEM.BZ2；队列保存、文件稳定检查、内容去重、失败重试，不再限最近 5 局或 48 小时。
- Demo 速度改为同一玩家连续位置/游戏时间差分，避免旧解析器速度字段的一 tick 错位。
- 官匹 DEM 与完美 ZIP 已完成真实本地记录配对验证；不匹配的同地图对局被拒绝。

## 仍有限制

- 采集页内存是采集进程工作集，不含统计窗口和临时 Demo 解析器。单局录制在内存累积，长局和解析时的峰值不能用空闲占用代替。正常退出等待保存；Windows 注销 / 关机做有界检查点，断电仍可能损失最近几秒，实际注销验收另计。
- 采集暂停是本次进程内状态；退出并重新启动采集时默认恢复。Hall 开关保存在 collector-settings.json。

- 参数建议目前是是否值得开始单项对照、还缺多少数据，以及方案间的描述性变化；不能从 gap / overlap 直接反推最佳触发深度或 RT。不同地图、对手、疲劳、练习和回报率等仍可造成差异。
- 趋势按同武器 / 姿态 / 初速段比较，3 局 / 30 次只作为收集基线的提示。单局不足 5 次的点标为空心，不参与波动比较。满 6 局后使用前后不重叠的多局中位数。未调整多重比较、未提供统计显著性或因果估计。
- 方案记录依赖用户在驱动实际应用后确认。程序不会自动读取当前触发 / RT，也不能察觉所有未登记的中途修改；这种对局需要标为未知。参数未知不参与方案比较，但保留表现统计。
- 输入时间线来自本地输入记录，鼠标按下不等于服务器开枪时刻；当前速度摘要无法区分开早与反向按久后重新加速。

- 急停意图无法直接观测。反向后 500 ms 内首击和反向前原方向分量用于确认可评估动作，不证明战术上需要急停。无反向首击、录制/同步缺失、再次换向仍不评估。手枪、冲锋枪等按具体武器分组，不共享步枪成绩；暂不提供枪械/姿态/距离专用精度模型。
- 0.5 u/s 是区分近零位置差分噪声的容差，不是游戏急停门槛。速度变化包含摩擦、蹲起、碰撞、受击和按键等共同影响；开镜/受击/转向保留上下文，不能从速度下降直接归因手指操作。未知姿态单列，不能伪装成站姿。
- 蹲伏速度上限缩放不等于完整蹲起模拟；Demo 检查连续 duck_amount 才能分组。短事件回推仍有时钟、tick 量化和输入到服务器时延，不能保证所有蹲起边界归类准确。没有足够合格样本时显示空值，不能由少量样本决定键盘触发/RT 参数。

- 统计窗口可关闭，但托盘采集需保持运行才能自动安排解析；退出托盘后，已开始的有限解析批次可完成，后续不再自动安排。不会代替用户登录、下载账号内录像。登录自启动可能被 Windows 禁用或延迟；登记启动项不等于完成下次登录验收。
- 每次扫描递归深度上限 10，跳过目录链接；默认位置外需手动添加目录。ZIP/BZ2 解压和失败任务会占用本地缓存，暂无缓存清理界面。
- 轮询文件稳定性及读共享锁降低半下载风险，解析失败会重试；不能证明所有下载器都使用标准临时文件命名。
- 队列一次一个解析器、低优先级与线程数限制。检测到 CS2 运行时取消正在进行的解析，保留待办；检测与取消有延迟，未做 FPS 对照测试。任务页的手动暂停在当前扫描结束后生效。
- 多类同步锚点共用时钟拟合，GSI 与 Mouse1 之间的来源时延尚未分开建模；MAD 后重新匹配仍可能加入一些偏离样本。必须区分全局同步残差与单次事件匹配残差，不能声称 sub-tick 时间精度。
- 速度是 tick 区间平均值；初速 34 u/s 是当前统计门槛，不是全枪械统一精度阈值。开枪时的历史 34 u/s 速度标记不代表成功率，也不替代反向前初速检查。后台复盘不把区间速度用于瞬时模型校准，保留原参数。
- 实时页仍显示输入概览/模型估算；统计页有更严格的首击筛选，因此两个页面的计数不必相同。没有本地录制的 Demo 无法还原键盘 gap/overlap。
- 模型缺少连续视角、碰撞和完整接地状态；实战可能没有足够可信模型样本，不能人为提高置信度规避。
- Hall 仅实测 41E4:2120 / 固件 0x0117。失联后需关闭再开启，旧键程清理和自动重连仍待完善。
- Hall 与官方驱动同时写配置存在竞争。仅设计上改变监测位，不能保证并发读改写不会覆盖他人的修改；建议先断开官方驱动连接。
- Hall debugMode 对输入延迟的影响、绝对机械精度及其他固件/灯光协议未验证。
- 原始会话仍每 5 秒整体写 JSON，已改流式序列化并释放结束的录制对象；超长连续录制仍需性能验证。统计 UI 读取独立小报告，后台只缓存会话索引，按需加载原始记录。

仓库和发布包不包含私人对局、Demo、GSI token 或本机分析数据库。
